<a name="functions"></a>

# 功能说明

[中文](/docs/zh-cn/FUNCTIONS.md) | [English](/FUNCTIONS.md) | [日本語](/docs/ja/FUNCTIONS.md)

YouTube Tweak 是一个用于增强 YouTube 网页版体验的浏览器扩展，在 Safari 版中显示为 YouTweak。本文按照扩展弹出面板里的分类顺序整理，重点说明每个功能有什么用、开启后会发生什么、适合什么场景，以及需要注意的限制。

本扩展主要在桌面版 YouTube 网页中生效。部分功能只在视频播放页、首页、搜索页、评论区或 Shorts 相关区域生效，具体范围会在对应功能里说明。

设置项旁的问号按钮可以直接打开本文对应位置。文中提到的默认状态，是指重置设置后使用的预设值；已有设置不会因为升级自动改变。

<a name="player"></a>

# 播放器

这一类功能用于增强视频播放页的观看体验，主要作用在 YouTube 播放器和视频播放页。

<a name="player-quality"></a>

## 锁定画质

<a name="player-quality-auto-switch"></a>

### 自动切换视频画质

默认: `关闭；目标画质为 1080p` | 版本：`v1.0.1 (2025-06-09)`

开启后，扩展会在每次进入或切换视频时，尝试把视频画质切换到你选择的清晰度。

适合的使用场景：

- 希望每个视频都尽量使用固定清晰度。
- 不想每次手动打开 YouTube 设置菜单调整画质。
- 网络环境稳定，想优先使用 1080p、1440p、4K 或更高画质。

可配置选项：

- 开关：控制是否自动切换视频画质。
- 目标画质：可选择 8K、4K、1440p、1080p、720p、480p、360p、240p 或 144p。

注意事项：

- 如果当前视频没有你选择的画质，扩展会自动降到这个画质以下可用的最高画质。
- 该功能只影响视频播放页。
- YouTube 本身可用的画质会受视频源、设备、网络和账号状态影响。

<a name="player-speed-button"></a>

## 倍速按钮

<a name="player-speed-button-show"></a>

### 显示倍速按钮和倍速滑块

默认: `开启；显示 0.5x、1x、1.5x、2x，倍速滑块开启，滚轮跟随按钮档位` | 版本：`v1.0.0 (2025-05-19)`

开启后，播放器控制栏里会显示自定义倍速按钮，用户可以直接点击按钮切换播放速度，不需要打开 YouTube 原本的设置菜单。

适合的使用场景：

- 经常在 1.5x、2x 等固定倍速之间切换。
- 看教程、直播回放、长视频时需要快速调速。
- 想把常用倍速放在播放器上直接点。

可配置选项：

- 显示倍速按钮/倍速滑块：控制播放器里是否显示扩展的倍速入口。
- 启用的按钮：可选择 0.25x、0.5x、0.75x、1x、1.25x、1.5x、1.75x、2x、2.25x、2.5x、2.75x、3x、5x、10x。
- 启用倍速滑块：开启后，可以按住倍速区域左右拖动，连续调整倍速。
- 鼠标滚轮步长：滚轮调速可以跟随已启用的倍速按钮，也可以使用 0.0625、0.125、0.25、0.5、1 或自定义步长。

注意事项：

- 倍速滑块依赖“显示倍速按钮/倍速滑块”开启，并且至少需要启用两个倍速按钮。
- 鼠标放在倍速区域滚动滚轮时，会直接调整播放速度。
- 自定义滚轮步长支持很细的数值，但过小会让调整变慢，过大会让速度跳变明显。

<a name="player-speed-button-save"></a>

### 记住播放速度

默认: `开启；按频道记忆开启` | 版本：`v1.0.1 (2025-06-09)`

开启后，扩展会记住你使用过的播放速度，并在之后的视频中自动恢复。

适合的使用场景：

- 总是用同一个倍速看大多数视频。
- 不同频道想使用不同倍速。
- 经常在学习类、娱乐类、音乐类频道之间切换。

可配置选项：

- 记住播放器速度：保存一个通用播放速度。
- 为每个频道分别记住播放速度：按频道保存播放速度。

注意事项：

- 同时开启通用记忆和分频道记忆时，分频道记忆优先。
- 分频道记忆需要扩展能识别当前视频所属频道。
- 记忆数据可以在通用设置里选择随设置一起导出。

<a name="player-volume-booster"></a>

## 音量增强器

<a name="player-volume-booster-gain"></a>

### 在播放器中增加真实音量增益

默认: `显示控制开启；音量增强关闭；增强级别 2x` | 版本：`v1.1.7 (2026-06-12)`

开启后，扩展可以在 YouTube 自带 100% 音量之上继续提高声音，并在播放器里显示一个音量增强按钮。

适合的使用场景：

- 视频本身声音太小。
- 系统音量已经调高，但 YouTube 仍然不够响。
- 想临时增强某个视频的音量，而不是调整整个系统。

可配置选项：

- 在倍速栏旁显示增强控制：控制播放器里是否显示音量增强按钮。
- 默认启用音量增强：进入视频时默认开启增强。
- 增强级别：可选择 1.25x 到 5x 的增益级别，每档间隔 0.25x。

使用方式：

- 点击播放器内的音量增强按钮，可以即时开关增强。
- 鼠标放在按钮上滚动滚轮，可以提高或降低增强级别。

注意事项：

- 声音增强过高可能导致破音、噪声或音质变差。
- 部分浏览器可能要求用户先和页面交互后，声音增强才会真正开始工作。
- 如果隐藏播放器内按钮，默认启用仍然可以生效，但不能在播放器里临时点击切换。

<a name="player-video-zoom"></a>

## 画面缩放

<a name="player-video-zoom-wheel"></a>

### 用鼠标滚轮缩放视频画面

默认: `开启` | 版本：`v1.1.8 (2026-07-03)`

开启后，可以在视频画面上滚动鼠标滚轮来放大或缩小画面。放大后，可以拖动画面来移动观看区域。

适合的使用场景：

- 视频里有小字、小细节或远处画面需要看清。
- 想放大横屏视频中的局部区域。
- 看教学、录屏、监控画面时需要临时观察细节。

使用方式：

- 鼠标移到视频画面中间区域，向上滚动放大。
- 放大后向下滚动缩小，直到恢复原大小。
- 放大后按住鼠标左键拖动，可以移动画面。

注意事项：

- 最大可放大到 4 倍。
- 鼠标在播放器按钮、控制栏、链接、输入框等区域上时，不会触发画面缩放。
- 视频处于原始大小时，只有画面中央出现缩放提示的区域会接管滚轮；其它区域仍可正常滚动页面。
- 该功能只作用于当前视频画面，不改变视频源文件。

<a name="player-subtitle"></a>

## 字幕

<a name="player-subtitle-save-status"></a>

### 记住字幕启用状态

默认: `开启；按频道记忆开启` | 版本：`v1.0.4 (2025-08-31)`

开启后，扩展会记录你是否打开了字幕，并在后续视频中尝试恢复字幕状态。

适合的使用场景：

- 常看外语视频，希望字幕默认保持打开。
- 部分频道需要字幕，部分频道不需要字幕。
- 不想每个视频都手动开关字幕。

可配置选项：

- 记住字幕启用状态：记录通用字幕状态。
- 为每个频道分别记住字幕启用状态：按频道记录字幕状态。

注意事项：

- 自动恢复字幕状态需要同时开启“记住字幕启用状态”和“为每个频道分别记住字幕启用状态”。
- 如果视频没有字幕，扩展无法强行生成 YouTube 字幕。
- 分频道记忆需要扩展能识别当前视频所属频道。

<a name="player-function-buttons"></a>

## 功能按钮

旋转、镜像和截图按钮可以分别开启。

<a name="player-function-buttons-rotate"></a>

### 旋转视频按钮

默认: `关闭` | 版本：`v1.0.9 (2025-11-22)`

开启后，播放器里会出现旋转按钮。每点击一次，视频画面旋转 90 度。

适合的使用场景：

- 视频方向上传错误。
- 想横屏观看竖屏内容，或临时调整观看方向。

注意事项：

- 旋转只影响当前页面里的显示效果，不会修改视频本身。
- 旋转 90 度或 270 度时，扩展会尽量缩放画面以适配播放器区域。

<a name="player-function-buttons-mirror"></a>

### 镜像视频按钮

默认: `关闭` | 版本：`v1.0.9 (2025-11-22)`

开启后，播放器里会出现镜像按钮。点击后可以水平翻转视频画面，再次点击可恢复。

适合的使用场景：

- 跟练舞蹈、健身、乐器动作时，希望左右方向更符合自己视角。
- 观看被镜像上传的视频时临时恢复方向。

注意事项：

- 镜像只影响显示效果，不会改变视频文件。
- 镜像可以和旋转同时使用。

<a name="player-function-buttons-screenshot"></a>

### 视频截图按钮

默认: `关闭` | 版本：`v1.1.8 (2026-07-03)`

开启后，播放器里会出现截图按钮。点击后会截取当前视频帧，并在新窗口中打开图片。

适合的使用场景：

- 保存视频中的某一帧画面。
- 做笔记、记录教程步骤或保存画面参考。

使用方式：

- 点击截图按钮后，扩展会打开一个新窗口显示截图。
- 在截图窗口中按 Ctrl+S 可以保存图片。

注意事项：

- 浏览器弹窗拦截可能会阻止截图窗口打开。
- 如果当前视频帧还没有准备好，截图可能失败。
- 截图文件名会尽量使用视频标题和当前播放时间。

<a name="player-button-display"></a>

## 按钮显示方式

<a name="player-button-display-native"></a>

### 控制播放器原生按钮显示

默认: `迷你播放器、画中画、剧场模式和电视播放按钮隐藏；其余自动` | 版本：`v1.0.0 (2025-05-19)`

该功能可以控制 YouTube 播放器中部分原生按钮的显示方式，让播放器控制栏更清爽。

适合的使用场景：

- 不使用某些 YouTube 原生按钮，希望减少干扰。
- 想强制显示某些被 YouTube 隐藏的按钮。
- 想让播放器控制栏更符合个人习惯。

可配置按钮：

- 自动播放按钮。
- 字幕按钮。
- 设置按钮。
- 迷你播放器按钮。
- 画中画按钮。
- 剧场模式按钮。
- 在电视上播放按钮。
- 全屏按钮。

可配置显示方式：

- 自动：保持 YouTube 默认显示方式。
- 隐藏：隐藏对应按钮。
- 强制启用：尽量让对应按钮保持可见。

注意事项：

- 字幕按钮只支持“自动”和“隐藏”，不提供“强制启用”。
- 如果 YouTube 页面结构变化，个别按钮的显示控制可能暂时失效。

<a name="player-mini-player"></a>

## 滚动迷你播放器

<a name="player-mini-player-scroll"></a>

### 滚动时让视频悬浮播放

默认: `关闭；480×270；右下角；边距 16px；触发距离 48px` | 版本：`v1.1.7 (2026-06-12)`

开启后，在视频播放页向下滚动离开播放器时，当前视频会变成悬浮迷你播放器，方便一边看视频一边浏览评论或推荐内容。

适合的使用场景：

- 看视频时想同时浏览评论区。
- 想在播放页往下翻内容，但不想让视频离开视线。
- 长视频播放时需要边看边找信息。

可配置选项：

- 大小：可选择 360×203、420×236、480×270、560×315、640×360、720×405。
- 位置：可选择左上、顶部居中、右上、左下、底部居中、右下。
- 屏幕边距：设置迷你播放器离屏幕边缘的距离。
- 滚动超过播放器后激活：设置离开原播放器多少像素后进入迷你播放器。

注意事项：

- 该功能只在普通视频播放页生效。
- 全屏播放或画中画播放时不会进入迷你播放器。
- 迷你播放器上有关闭按钮，点击后本次滚动中会关闭悬浮；当你滚回播放器附近或切换视频后，会重新允许触发。
- 屏幕太小时，实际显示尺寸会自动缩小以避免超出屏幕。
- 小尺寸迷你播放器会隐藏部分不适合显示的控制按钮。

<a name="player-ui"></a>

## 界面

<a name="player-ui-ending-overlay"></a>

### 半透明结尾推荐内容

默认: `开启` | 版本：`v1.0.0 (2025-05-19)`

开启后，视频结尾覆盖在画面上的推荐视频、作者入口等元素会变成半透明，减少遮挡。

适合的使用场景：

- 经常被视频结尾推荐卡片挡住画面。
- 想看清视频最后几秒的内容。

注意事项：

- 鼠标悬停到这些推荐元素上时，它们会恢复正常显示，方便点击。
- 该功能只影响播放器中的结尾覆盖元素。

<a name="player-ui-progress-bar"></a>

### 视频下方进度条

默认: `开启；高度 2px` | 版本：`v1.0.8 (2025-11-14)`

开启后，当 YouTube 播放器控制栏自动隐藏时，视频底部会显示一条简洁进度条。

适合的使用场景：

- 想在不显示完整控制栏的情况下看到播放进度。
- 想减少 YouTube 原生进度条和控制栏的遮挡。

可配置选项：

- 是否显示进度条。
- 进度条高度，支持 1 到 20 像素。

注意事项：

- 进度条主要在播放器控制栏隐藏时显示。
- 直播视频不会显示这条进度条。

<a name="player-ui-time-tag"></a>

### 角落视频时间

默认: `开启；字号 12；左下角；边距 5px` | 版本：`v1.0.8 (2025-11-14)`

开启后，当播放器控制栏自动隐藏时，播放器角落会显示当前播放时间和总时长。

适合的使用场景：

- 想随时知道播放到哪里，但不想打开完整控制栏。
- 看长视频时需要更明显的时间提示。

可配置选项：

- 是否显示视频时间。
- 字体大小，支持 8 到 48。
- 显示位置：左下、右下、左上、右上。
- 距离边缘的偏移，支持 0 到 200 像素。

注意事项：

- 时间角标主要在播放器控制栏隐藏时显示。
- 直播视频通常只显示当前播放时间。

<a name="player-other"></a>

## 其他

<a name="player-other-max-volume"></a>

### 最大音量时使用真正 100% 音量

默认: `开启` | 版本：`v1.0.0 (2025-05-19)`

开启后，当 YouTube 音量条处于 100% 时，扩展会确保视频元素本身也处于真正的满音量状态。

适合的使用场景：

- YouTube 显示音量已满，但实际声音仍偏小。
- 想确保播放器没有被额外压低音量。

注意事项：

- 该功能只在 YouTube 音量条为 100% 时介入。
- 这不是额外放大音量；如果需要超过 100% 的音量，请使用“音量增强器”。
- 生效时音量滑块会用醒目的颜色提示。

<a name="player-other-nonstop"></a>

### 禁用自动暂停视频

默认: `开启` | 版本：`v1.1.5 (2026-04-11)`

开启后，扩展会尽量阻止 YouTube 长时间播放后的自动暂停提示，并在检测到“视频已暂停，继续播放？”这类弹窗时自动处理。

适合的使用场景：

- 长时间播放音乐、播客、直播回放或学习视频。
- 不想 YouTube 因为长时间没有操作而暂停。

注意事项：

- 该功能只针对 YouTube 自动暂停类行为。
- 如果视频因为用户手动暂停、网络问题或页面限制而停止播放，扩展不会保证继续播放。

<a name="translate"></a>

# 翻译

这一类功能用于翻译视频列表标题、播放页文字、字幕和评论。所有翻译功能都会使用在线翻译服务，因此需要网络可用。

<a name="translate-settings"></a>

## 翻译设定

<a name="translate-settings-target-language"></a>

### 翻译目标语言

默认: `跟随 YouTube 语言` | 版本：`v1.0.9 (2025-11-22)`

该设置决定视频列表、播放页、字幕和评论要翻译成哪种语言。

可配置选项：

- 跟随 YouTube 语言：自动使用当前 YouTube 页面语言。
- 指定语言：从列表中选择固定目标语言。

注意事项：

- 如果原文语言和目标语言相同，部分翻译功能会跳过自动翻译。
- 更改目标语言后，已显示的旧翻译内容可能需要刷新页面或重新加载评论后才会更新。

<a name="translate-settings-never-translate"></a>

### 始终不翻译的语言

默认: `空列表` | 版本：`v1.1.2 (2025-11-26)`

该设置用于避免自动翻译某些语言的内容，适用于视频列表标题、播放页标题、简介、摘要、字幕和评论。

适合的使用场景：

- 你能直接阅读某些语言，不希望它们被自动翻译。
- 想避免中文评论、英文评论或其它熟悉语言重复出现译文。

注意事项：

- 语言判断会忽略同一种语言的地区差异，例如选择英语后，也会跳过其它地区的英语内容。
- 当评论被识别为不需要翻译的语言时，只有开启“为无需自动翻译的评论显示手动翻译按钮”，扩展才会保留手动查看译文的入口。
- 视频列表、播放页文字和字幕没有手动翻译入口；匹配排除语言时会直接保留原文。

<a name="translate-global"></a>

## 全局翻译

<a name="translate-global-video-list-title"></a>

### 翻译视频列表标题

默认: `开启` | 版本：`v1.2.1 (2026-07-21)`

开启后，扩展会在视频列表的原标题下方显示译文，方便在浏览页面时直接理解视频内容，不必先进入播放页。

适合的使用场景：

- 浏览外语首页、搜索结果或推荐列表。
- 查看 Shorts 标题和播放器结尾推荐时，希望同时看到译文。
- 想保留原标题，方便对照原文和译文。

生效范围：

- YouTube 首页、搜索结果、播放页推荐列表等使用视频卡片的区域。
- Shorts 视频卡片。
- 播放器结尾显示的推荐视频。

注意事项：

- 该功能不依赖字幕翻译或评论翻译开关。
- 译文使用“翻译目标语言”中的设置；选择“跟随 YouTube 语言”时，会使用当前 YouTube 页面语言。
- 原标题会保留，译文显示在原标题下方，并使用虚线下划线帮助区分。
- 如果标题已经是目标语言、译文与原文相同或翻译失败，扩展不会重复显示译文。
- 开启或关闭该功能时，当前 YouTube 页面会自动刷新。

<a name="translate-watch"></a>

## 播放页翻译

<a name="translate-watch-video-title"></a>

### 翻译播放页视频标题

默认: `开启` | 版本：`v1.2.1 (2026-07-21)`

开启后，普通视频播放页会在原标题下方显示标题译文，原标题仍会保留。

适合的使用场景：

- 观看外语视频时快速理解标题。
- 需要对照原标题与译文，避免只看译文失去原始语境。

注意事项：

- 该功能只在普通视频播放页生效。
- 译文使用“翻译目标语言”中的设置，不依赖简介、摘要、字幕或评论翻译开关。
- 如果标题已经是目标语言、译文与原文相同或翻译失败，页面会保持原标题。
- 开启或关闭该功能时，当前 YouTube 页面会自动刷新。

<a name="translate-watch-video-description"></a>

### 翻译视频简介

默认: `开启` | 版本：`v1.2.1 (2026-07-21)`

开启后，普通视频播放页会在原简介附近显示翻译后的简介。简介处于折叠或展开状态时，都可以看到对应译文。

适合的使用场景：

- 阅读外语视频的内容说明、章节介绍或补充资料。
- 希望保留原简介中的链接、图片和排版，同时阅读译文。

注意事项：

- 该功能只在普通视频播放页生效。
- 译文使用“翻译目标语言”中的设置，不依赖标题、摘要、字幕或评论翻译开关。
- 原简介会保留，译文会放在带虚线边框的区域中；链接、图片等不能直接翻译的内容会保持原样。
- 如果简介没有可翻译文字或翻译失败，页面会保持原简介。
- 开启或关闭该功能时，当前 YouTube 页面会自动刷新。

<a name="translate-watch-video-summary"></a>

### 翻译视频摘要

默认: `开启` | 版本：`v1.2.1 (2026-07-21)`

开启后，如果 YouTube 在普通视频播放页提供了视频摘要，扩展会在原摘要附近显示摘要译文。摘要折叠和展开时都会同步显示合适的译文。

适合的使用场景：

- 想先通过摘要了解外语视频的大致内容。
- YouTube 已显示摘要，但摘要语言不便阅读。

注意事项：

- 该功能只在普通视频播放页且 YouTube 已提供摘要时生效。
- 扩展只翻译现有摘要，不会为没有摘要的视频自动生成摘要。
- 译文使用“翻译目标语言”中的设置，不依赖标题、简介、字幕或评论翻译开关。
- 如果摘要已经是目标语言、译文与原文相同或翻译失败，页面会保持原摘要。
- 开启或关闭该功能时，当前 YouTube 页面会自动刷新。

<a name="translate-subtitle"></a>

## 字幕翻译

<a name="translate-subtitle-enable"></a>

### 开启字幕翻译

默认: `开启；双语翻译` | 版本：`v1.1.3 (2025-12-03)`

开启后，扩展会在 YouTube 加载字幕时翻译字幕内容，并把译文显示到播放器字幕里。

适合的使用场景：

- 看外语视频时需要字幕翻译。
- 想保留原字幕，同时看到译文。
- 想直接把字幕替换成目标语言。

可配置选项：

- 开启字幕翻译：控制是否翻译视频字幕。
- 翻译方式：可选择“双语翻译”或“仅显示译文”。

翻译方式说明：

- 双语翻译：在字幕中同时显示译文和原文。
- 仅显示译文：只显示翻译后的内容。

注意事项：

- 该功能需要视频本身有可用字幕。
- 开启或关闭字幕翻译可能会刷新当前 YouTube 页面。
- 字幕内容过长或翻译服务失败时，扩展会保留原字幕。
- 字幕翻译属于会调整页面加载内容的功能，若页面出现异常，可先关闭该功能排查。

<a name="translate-comment"></a>

## 评论翻译

<a name="translate-comment-content"></a>

### 翻译评论内容

默认: `开启` | 版本：`v1.0.0 (2025-05-19)`

开启后，扩展会自动翻译评论区中的评论，并在评论原文附近显示译文。

适合的使用场景：

- 浏览多语言评论区。
- 看海外视频时想快速理解评论内容。
- 不想逐条点击 YouTube 原生翻译按钮。

可配置选项：

- 翻译评论内容：控制是否自动翻译评论。
- 为无需自动翻译的评论显示手动翻译按钮：当评论语言与目标语言相同，或属于“始终不翻译的语言”时，保留手动查看译文的入口。
- 翻译目标语言：决定评论翻译成哪种语言。
- 始终不翻译的语言：避免熟悉语言自动显示译文。

注意事项：

- 如果评论为空或只有符号，扩展会显示手动翻译按钮。被识别为不需要翻译的评论只有在开启“为无需自动翻译的评论显示手动翻译按钮”后才会显示该按钮。
- 译文会尽量保留原评论中的表情、图片、链接和文字样式；译文里的视频时间链接仍可点击跳转到对应时间。
- 评论排序、加载更多评论或 YouTube 动态刷新评论区后，扩展会继续处理新出现的评论。
- 如果翻译服务不可用，评论会保持原样。

<a name="translate-comment-manual-button"></a>

### 为无需自动翻译的评论显示手动翻译按钮

默认: `开启` | 版本：`v1.2.1 (2026-07-21)`

开启后，当评论语言与翻译目标语言相同，或属于“始终不翻译的语言”时，评论旁仍会显示手动翻译按钮。

关闭后，这些评论既不会自动显示译文，也不会显示手动翻译按钮。真正需要翻译的评论仍会正常自动翻译。

<a name="translate-comment-line-by-line"></a>

### 译文逐行显示在原文下方

默认: `开启` | 版本：`v1.1.7 (2026-06-12)`

开启后，对于包含多行内容的评论，扩展会尽量把每行译文放到对应原文下方，而不是把整段译文堆在评论末尾。

适合的使用场景：

- 翻译歌词、分段说明、长评论或多行时间轴。
- 希望原文和译文能逐行对照阅读。

注意事项：

- 该功能依赖“翻译评论内容”开启。
- 只有评论本身包含换行时，逐行显示才明显；普通单行评论仍会按普通方式显示译文。

<a name="other"></a>

# 其他

这一类功能用于调整 YouTube 页面布局、评论显示、广告干扰、Shorts、品牌外观、旧版布局和自定义样式。

<a name="other-home"></a>

## 首页

<a name="other-home-video-per-row"></a>

### 锁定首页推荐视频每行数量

默认: `关闭；每行 4 个` | 版本：`v1.0.0 (2025-05-19)`

开启后，可以固定 YouTube 首页推荐视频每行显示的数量。

适合的使用场景：

- 想让首页卡片更密集或更宽松。
- 不喜欢 YouTube 自动根据窗口宽度调整每行数量。
- 大屏幕上想显示更多视频。

可配置选项：

- 开关：控制是否锁定每行数量。
- 数量：输入每行显示的视频数量。

注意事项：

- 该功能主要影响首页推荐视频网格。
- 当前输入范围建议为 1 到 10；超出范围会被重置为 4。
- 页面宽度过窄时，YouTube 自身布局仍可能影响实际显示效果。

<a name="other-comment"></a>

## 评论

<a name="other-comment-nickname"></a>

### 显示评论者昵称

默认: `开启` | 版本：`v1.0.0 (2025-05-19)`

开启后，扩展会尝试显示评论者的频道名称，并把原本的账号名作为辅助信息显示。

适合的使用场景：

- 想看评论者更完整的频道显示名。
- 不习惯只看 YouTube 的账号名或句柄。

注意事项：

- 开启或关闭后可能需要刷新 YouTube 页面。
- 该功能需要访问评论者的频道页面来读取名称，因此加载速度取决于网络和 YouTube 页面响应。
- 如果频道信息无法读取，评论显示会保持 YouTube 原样。

<a name="other-comment-auto-more"></a>

### 自动展开长评论

默认: `开启` | 版本：`v1.0.0 (2025-05-19)`

开启后，长评论会自动展开，评论里的“显示更多”和“收起”按钮会被隐藏。

适合的使用场景：

- 经常阅读长评论或歌词评论。
- 不想反复点击“显示更多”。

注意事项：

- 该功能主要影响评论区。
- 开启后长评论默认展开，手动收起入口会被隐藏。

<a name="other-anti-ad"></a>

## 广告屏蔽

<a name="other-anti-ad-image"></a>

### 屏蔽图文广告

默认: `关闭` | 版本：`v1.0.0 (2025-05-19)`

开启后，扩展会隐藏 YouTube 页面中的图文广告、广告位、部分 Premium 推广和页面广告模块。

适合的使用场景：

- 想减少首页、播放页和侧栏中的广告干扰。
- 想让推荐内容区域更干净。

注意事项：

- 这是实验功能，不保证能屏蔽所有广告。
- YouTube 页面结构变化后，部分广告可能重新出现。

<a name="other-anti-ad-video"></a>

### 视频广告屏蔽（已停用）

默认: `不可用` | 版本：`v1.0.0 (2025-05-19)`

当前版本已停用扩展内置的视频广告屏蔽功能。设置面板中的入口会显示为不可选择的划线选项，无法再开启。

可选方案：

- uBlock Origin：用于屏蔽包括视频广告在内的常见网页广告。
- SponsorBlock：用于跳过视频内容中由作者加入的赞助或推广片段。

注意事项：

- 上述方案是独立的浏览器扩展，不属于 YouTube Tweak，是否可用取决于浏览器和对应扩展的支持情况。
- “屏蔽图文广告”和“屏蔽频道主商品推荐”仍是可以单独使用的功能。

<a name="other-anti-ad-merch"></a>

### 屏蔽频道主商品推荐

默认: `关闭` | 版本：`v1.0.0 (2025-05-19)`

开启后，扩展会隐藏频道主商品推荐、购物推荐栏、票务推荐和播放器中的部分商品推荐入口。

适合的使用场景：

- 不想在播放页看到商品货架或购物推广。
- 想减少视频周边商业推荐。

注意事项：

- 该功能只隐藏相关页面元素，不改变视频内容本身。
- YouTube 页面结构变化后，个别商品模块可能暂时无法隐藏。

<a name="other-shorts"></a>

## Shorts（短视频）

三个 Shorts 屏蔽选项彼此独立。

<a name="other-shorts-home-search"></a>

### 屏蔽首页和搜索结果中的 Shorts

默认: `关闭` | 版本：`v1.0.1 (2025-06-09)`

开启后，首页推荐中的 Shorts 区块会被隐藏，搜索结果中的 Shorts 区块也会一起隐藏。

适合的使用场景：

- 不想在首页看到 Shorts 推荐。
- 搜索内容时只想看普通视频结果。

注意事项：

- 该功能影响首页和搜索页中的 Shorts 推荐区域。
- 它不会删除 YouTube 上的 Shorts 内容，只是隐藏对应入口或区块。

<a name="other-shorts-watch"></a>

### 屏蔽视频播放页中的 Shorts 推荐

默认: `关闭` | 版本：`v1.0.1 (2025-06-09)`

开启后，视频播放页侧栏、简介区域或相关推荐区域中的 Shorts 推荐会被隐藏。

适合的使用场景：

- 看普通视频时不想被 Shorts 推荐打断。
- 想让播放页推荐区更专注于普通视频。

<a name="other-shorts-menu"></a>

### 在侧边栏里屏蔽 Shorts

默认: `关闭` | 版本：`v1.0.1 (2025-06-09)`

开启后，YouTube 侧边栏、迷你侧边栏以及移动网页版底部导航中的 Shorts 入口会被隐藏。

适合的使用场景：

- 不使用 Shorts，希望减少入口干扰。
- 想避免误点 Shorts。

注意事项：

- 该功能主要隐藏入口，不影响直接打开 Shorts 链接。
- YouTube 不同语言或布局下，个别入口可能无法隐藏。

<a name="other-rollback"></a>

## 回退

<a name="other-rollback-player-layout"></a>

### 回到旧版播放界面布局

默认: `关闭` | 版本：`v1.1.7 (2026-06-12)`

开启后，扩展会把 YouTube 播放页的一部分布局调整得更接近旧版样式，例如恢复较窄的右侧推荐栏和较小的推荐缩略图。

适合的使用场景：

- 不喜欢新版播放页右侧推荐过宽或缩略图过大。
- 希望播放页布局更紧凑。
- 遇到非 16:9 视频在播放器区域中显示不协调。

注意事项：

- 该功能主要影响桌面版视频播放页的双栏布局。
- 它是对页面外观的回退调整，YouTube 页面改版后效果可能变化。

<a name="other-appearance"></a>

## 外观

<a name="other-appearance-premium-logo"></a>

### 伪装 Premium Logo

默认: `关闭` | 版本：`v1.2.0 (2026-07-18)`

开启后，左上角的普通 YouTube Logo 会显示为 YouTube Premium Logo。

适合的使用场景：

- 更喜欢 YouTube Premium Logo 的样式。
- 希望页面顶栏看起来像 Premium 账号。

注意事项：

- 该功能只改变 Logo 外观，不会开通 YouTube Premium、改变账号会员状态或解锁任何 Premium 权益。
- 开启或关闭该功能时，当前 YouTube 页面会自动刷新。
- YouTube 调整页面数据结构后，Logo 可能暂时无法替换。

<a name="other-appearance-logo-country-code"></a>

### 修改 Logo 国家/地区标识

默认: `空` | 版本：`v1.2.1 (2026-07-21)`

填写文本后，YouTube Logo 旁原本用于显示国家或地区的标识会改成你填写的内容。留空后会恢复 YouTube 默认显示。

适合的使用场景：

- 想自定义页面左上角的品牌标识。
- 想把默认国家或地区缩写改成个性化文字。

可配置选项：

- 国家/地区标识：可以填写自定义文本，例如 `Pirates`；留空表示不修改。

注意事项：

- 该功能只改变 Logo 旁的显示文字，不会更改账号地区、内容区域、语言、货币、推荐结果或实际所在地。
- 修改或清空内容后，已打开的 YouTube 页面会自动刷新。
- 这个选项可以单独使用，不依赖“伪装 Premium Logo”；两者同时开启时也可以共同生效。
- YouTube 调整页面布局后，这段标识可能暂时无法显示。

<a name="other-custom-css"></a>

## 自定义 CSS

<a name="other-custom-css-inject"></a>

### 在 YouTube 注入自定义 CSS

默认: `关闭；样式内容为空` | 版本：`v1.1.8 (2026-07-03)`

开启后，可以把自己写的 CSS 样式应用到 YouTube 页面，用来调整颜色、隐藏元素或修改布局细节。

适合的使用场景：

- 你熟悉 CSS，想按个人习惯微调 YouTube。
- 想临时隐藏某些页面元素。
- 想做更深度的个性化外观调整。

可配置选项：

- 开关：控制是否启用自定义样式。
- 文本框：填写要应用到 YouTube 的 CSS。

注意事项：

- 文本框失去焦点后会保存内容，并应用到已打开的 YouTube 页面。
- 写错 CSS 可能导致 YouTube 页面显示异常。
- 关闭开关后，自定义样式会从页面中移除。

<a name="general"></a>

# 通用

这一类功能用于扩展本身的语言、设置、更新、关于信息和安装提示。

<a name="general-about"></a>

## 关于

<a name="general-about-info"></a>

### 查看扩展信息和项目入口

默认: `始终显示` | 版本：`v1.0.0 (2025-05-19)`

通用页面会显示扩展名称、版本号、构建时间和当前版本对应的提交，还提供项目主页、更新日志、问题反馈和赞助入口。Safari 版会使用 YouTweak 名称和对应图标，其它版本显示 YouTube Tweak。

点击赞助入口会显示赞助提示卡，也可以直接关闭这张提示卡。

适合的使用场景：

- 想确认当前安装的版本。
- 想查看更新内容。
- 想反馈问题或查看项目源码。

<a name="general-about-check-update"></a>

### 手动检查更新

默认: `点击版本号手动触发` | 版本：`v1.0.9 (2025-11-22)`

点击版本号可以手动检查扩展是否有新版本。

注意事项：

- 如果浏览器不支持手动检查更新，扩展会提示你去扩展商店查看。
- 如果检查过于频繁，浏览器可能会暂时限制再次检查。

<a name="general-language"></a>

## 语言

<a name="general-language-switch"></a>

### 切换扩展显示语言

默认: `跟随浏览器语言；不支持时使用英语` | 版本：`v1.0.1 (2025-06-09)`

可以在通用页面切换扩展弹出面板的语言。

适合的使用场景：

- 想使用自己熟悉的语言查看设置。
- 想帮助检查某个翻译是否准确。

注意事项：

- 语言切换会立即影响扩展面板。
- 页面中提供翻译贡献者入口，方便查看或参与翻译。

<a name="general-update-notice"></a>

## 更新

<a name="general-update-notice-disable"></a>

### 禁用更新提示

默认: `关闭` | 版本：`v1.2.1 (2026-07-21)`

开启后，扩展不再用扩展图标上的 `new` 标记提醒你阅读新版本说明，点击扩展图标时也不会因为有未读说明而自动打开更新日志。

适合的使用场景：

- 不希望扩展图标显示新版本说明标记。
- 希望每次点击扩展图标时直接进入平常使用的设置页面。

注意事项：

- 关闭该开关时，会显示未读的新版本说明提醒。
- 未读标记按版本号的前三段判断；如果更新只改变第四段修订号，不会再次显示 `new` 标记。
- 这个选项只关闭新版本说明提醒，不会阻止浏览器检查、下载或安装扩展更新。
- 关闭提醒后，仍然可以在“关于”中手动查看更新日志，也可以点击版本号手动检查更新。

<a name="general-update-notice-version"></a>

### 可用更新提示

默认: `检测到新版本时自动显示` | 版本：`v1.0.1 (2025-06-09)`

当浏览器检测到扩展有可安装的新版本时，通用页面会显示更新提示。

可用操作：

- 查看更新日志。
- 立即更新。

注意事项：

- 正常情况下，关闭所有 YouTube 标签后浏览器会自动完成扩展更新。
- 点击“立即更新”会让扩展重新加载，并在更新完成后刷新已打开的 YouTube 标签页。
- “禁用更新提示”只关闭未读的新版本说明提醒，不会关闭浏览器已经检测到可用更新时的提示。

<a name="general-update-notice-reload-page"></a>

### YouTube 页面需要重新加载提示

默认: `自动检测并提示` | 版本：`v1.0.1 (2025-06-09)`

当扩展更新后，已经打开的 YouTube 页面可能还在使用旧状态。此时页面上可能出现提示，要求重新加载页面。

适合的使用场景：

- 扩展刚更新，但 YouTube 页面里的功能看起来异常。
- 页面提示扩展已更新，需要重新加载。

注意事项：

- 设置面板中没有单独开关。
- 点击提示里的重新加载按钮即可刷新当前页面。
- 重新加载后，页面会使用新的扩展状态。

<a name="general-update-notice-compatibility"></a>

### 兼容性警告

默认: `开启字幕翻译或伪装 Premium Logo 时显示` | 版本：`v1.1.3 (2025-12-03)`

当开启字幕翻译或伪装 Premium Logo 时，通用页面会显示警告提示。

原因说明：

- 这些功能会调整 YouTube 加载到页面中的字幕或品牌信息。
- 这种调整可能因为 YouTube 改版、网络问题或浏览器限制导致页面异常。

建议：

- 如果遇到视频无法播放、字幕异常、页面加载异常，或购买 Premium、加入频道会员、直播打赏等支付相关页面出现问题，先关闭字幕翻译和伪装 Premium Logo 进行排查。

<a name="general-config"></a>

## 插件设置

<a name="general-config-export"></a>

### 导出设置

默认: `仅导出普通设置；不包含记忆` | 版本：`v1.0.1 (2025-06-09)`

可以把当前扩展设置导出为文本，方便备份、迁移到其他浏览器，或分享给其他用户。

可配置选项：

- 同时导出记忆：导出分频道倍速、字幕状态等记忆数据。

注意事项：

- 如果不勾选“同时导出记忆”，只会导出普通设置。
- 导出的内容会复制到剪贴板；如果复制失败，可以手动选择文本复制。

<a name="general-config-import"></a>

### 导入设置

默认: `仅导入普通设置；记忆需要另行确认` | 版本：`v1.0.1 (2025-06-09)`

可以粘贴之前导出的设置文本，将设置恢复到当前浏览器。

注意事项：

- 导入失败时，通常是文本格式不正确或内容不完整。
- 如果导入内容包含记忆数据，面板会要求你额外确认是否导入这些记忆。
- 导入记忆可能覆盖当前浏览器已有的分频道倍速、字幕等记忆。

<a name="general-config-reset"></a>

### 重置设置

默认: `恢复普通设置；保留分频道记忆` | 版本：`v1.0.0 (2025-05-19)`

可以把扩展设置恢复为默认值。

注意事项：

- 重置前会弹出确认提示。
- 重置后面板会回到“播放器”页面。
- 重置普通设置不会专门清空分频道记忆；如果要迁移或覆盖记忆，请使用导入导出里的记忆选项。

<a name="general-installed"></a>

## 安装欢迎页

<a name="general-installed-first-run"></a>

### 首次安装提示

默认: `首次安装后自动打开` | 版本：`v1.0.0 (2025-05-19)`

首次安装扩展后，会打开一个欢迎页面，提示扩展已安装成功，并引导用户点击浏览器的扩展图标开始使用。页面会根据 Safari、Chrome、Firefox、Edge 或其它浏览器调整箭头和扩展图标提示的位置与样式。

适合的使用场景：

- 刚安装扩展，需要知道从哪里打开设置面板。
- 想确认扩展已经安装完成。
