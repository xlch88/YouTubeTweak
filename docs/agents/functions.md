你需要完整扫描当前项目的全部代码，并在项目 `docs/zh-cn/` 目录创建或覆盖 `FUNCTIONS.md`。

这份文档必须使用中文编写，目标读者是普通用户，不是开发者。用户只需要知道这个浏览器扩展有哪些功能、每个功能怎么用、适合什么场景、开启后会发生什么，不需要知道任何代码、文件路径、组件名、函数名、变量名、实现细节。

## 核心目标

根据项目真实代码逻辑，整理出扩展的全部功能，并按照扩展配置页面的结构进行分类。

最终文档结构必须尽量贴近 popup 配置面板中的分类顺序，格式为：

`大分类 -> 小分类 -> 功能`

例如：

```

# 播放器

这一类功能用于增强视频播放页面的使用体验。

## 锁定画质

### 自动切换视频画质

说明这个功能是什么、开启后会做什么、适合什么场景、有什么限制或注意事项。

```

## 分析范围

必须重点阅读并结合以下目录和文件：

1. `src/entrypoints/popup/pages`
    - 这是扩展的配置页面。
    - 需要根据这里的页面结构、分组、开关、选项，确定文档的大分类、小分类和功能顺序。

2. `src/inject/plugins`
    - 这是各个功能的主要实现位置。
    - 需要根据真实逻辑理解功能实际做了什么。

3. `src/inject/plugins_before`
    - 这是部分提前注入功能的实现位置。
    - 需要一并分析，不能遗漏。

4. `src/defaultConfig.ts`
    - 这是扩展的全部默认配置项。
    - 需要用它校对所有功能、开关、子选项、默认行为，避免遗漏隐藏配置。

除了以上重点位置，也需要扫描项目内其他相关代码，尤其是：

- 配置项被读取的位置
- 功能开关判断逻辑
- 功能之间的依赖关系
- 只在特定页面生效的逻辑
- 默认值、限制条件、特殊行为

还必须扫描完整 Git 提交历史，并结合以下内容确认每个功能最早出现的版本：

- 功能相关代码、配置项和界面入口最早出现的提交
- Git 版本标签
- `package.json` 中的版本变更提交
- 项目更新日志

不能只根据当前文件的 `git blame` 或当前版本号判断功能版本。如果功能代码在两个发行版本之间加入，应记录第一个包含该功能的发行版本；如果对应版本没有 Git 标签，需要结合 `package.json` 的版本变更提交和更新日志判断。

## 文档要求

`FUNCTIONS.md` 必须满足以下要求：

1. 必须覆盖项目中所有用户可感知的功能。
2. 必须按照 popup 面板的分类结构组织内容。
3. 必须使用通俗、直接、易懂的中文。
4. 不要写代码。
5. 不要出现函数名、变量名、类名、组件名、实现文件路径等开发者内容。
6. 不要解释技术实现原理。
7. 不要使用晦涩术语。无法避免时，要用普通话解释清楚。
8. 每个功能都要说明：
    - 这个功能是什么
    - 开启后会发生什么
    - 用户什么时候会用到它
    - 有哪些可配置选项
    - 相关选项之间是否有依赖
    - 是否只在特定页面生效
    - 是否有注意事项或限制
9. 如果某个功能包含多个子选项，必须分别说明每个子选项的作用。
10. 如果某个功能需要另一个开关开启后才生效，必须明确写出依赖关系。
11. 如果某个功能会影响 YouTube 的不同页面，例如首页、搜索页、播放页、频道页、Shorts 页面、播放列表页面等，必须写清楚生效范围。
12. 如果某个配置项只影响显示效果、交互方式、快捷操作、默认行为，也必须单独说明。
13. 不要编造代码中不存在的功能。
14. 如果发现配置页面中有功能入口，但实现逻辑不完整，要用用户能理解的方式描述现有可见效果，不要写“代码未实现”这种开发者话术。
15. 最终只输出 `FUNCTIONS.md` 文件，不需要创建其他文档。

## 默认值和版本号要求

每个 `### 功能名称` 标题下面都必须紧接着写一行当前默认值和功能最早出现的版本，格式固定为：

```markdown
### 功能名称

默认: `value` | 版本：v1.x.x

说明这个功能的主要用途。
```

具体要求：

1. 默认值必须以当前 `src/defaultConfig.ts` 和真实代码行为为准。
2. 默认值是用户重置设置后得到的当前预设值，不要按安装时间描述。
3. 单个开关使用 `开启` 或 `关闭`。
4. 功能包含多个默认选项时，使用中文分号合并到同一对反引号中，例如：

    ```markdown
    默认: `关闭；目标画质为 1080p` | 版本：v1.0.1
    ```

5. 没有用户可配置开关的功能，需要根据实际行为填写触发条件，例如 `始终显示`、`检测到新版本时自动显示`。
6. 已停用或无法使用的功能填写 `不可用`，不要伪造开关状态。
7. 版本号表示该功能第一次对用户可用的发行版本，不是当前扩展版本，也不是最近修改该功能的版本。
8. 如果一个功能后来增加了子选项或增强行为，仍以这个基础功能最早出现的发行版本为准。
9. 版本号统一只保留前三段，必须写成 `v1.x.x`，不要写第四段修订号。
10. 无法从单一提交确认版本时，必须用功能引入提交、版本标签、`package.json` 版本变更和更新日志交叉判断，不能猜测。
11. 同一项默认信息只在标题下方的标准行中写一次，正文不要重复描述。

## 写作风格

文档要像产品说明书，不要像技术文档。

错误示例：

> 该功能通过监听 DOM 变化并调用播放器 API 修改 playbackRate。

正确示例：

> 开启后，扩展会在视频播放器中显示倍速控制入口，用户可以直接用鼠标切换播放速度，不需要打开 YouTube 原本的设置菜单。

错误示例：

> 读取 config.enableSpeedSlider 后初始化 SpeedSliderPlugin。

正确示例：

> 开启后，可以通过拖动倍速控制条来调整播放速度，也可以把鼠标放在倍速区域上滚动滚轮快速调整。

## 推荐的功能说明格式

每个功能建议使用下面的结构，但可以根据实际情况调整：

```markdown
### 功能名称

默认: `value` | 版本：v1.x.x

说明这个功能的主要用途。

开启后会发生什么。

适合的使用场景：

- 场景一
- 场景二

可配置选项：

- 选项一：说明作用。
- 选项二：说明作用。
- 选项三：说明作用。

注意事项：

- 如果没有相关注意事项，可以省略这一段。
- 如果该功能依赖其他开关，必须写在这里。
```

## 分类要求

必须优先根据 popup 配置页面中的真实面板结构分类。

如果 popup 中存在类似以下结构，需要按实际项目中的名称整理：

```markdown
# 播放器

## 画质

### 自动锁定画质

# 外观

## 页面布局

### 隐藏指定模块

# Shorts

## Shorts 播放器

### 修改 Shorts 行为
```

不要把所有功能堆成一个大列表。

不要按代码目录分类。

不要按实现文件分类。

用户看到的是配置面板，所以文档也要跟配置面板一致。

## 检查要求

写入 `FUNCTIONS.md` 前必须完成以下检查：

1. 是否已经扫描 `src/entrypoints/popup/pages`。
2. 是否已经扫描 `src/inject/plugins`。
3. 是否已经扫描 `src/inject/plugins_before`。
4. 是否已经扫描 `src/defaultConfig.ts`。
5. 是否存在配置项没有被写进文档。
6. 是否存在 popup 页面中的开关没有被写进文档。
7. 是否存在插件功能没有被写进文档。
8. 是否存在功能依赖关系没有说明。
9. 是否存在只在特定页面生效的功能没有说明生效范围。
10. 是否出现了代码、变量名、函数名、文件路径等不该给用户看的内容。
11. 是否每个 `###` 功能标题下方都有且只有一条 ``默认: `value` | 版本：v1.x.x``。
12. 是否所有默认值都与当前 `src/defaultConfig.ts` 和真实代码行为一致。
13. 是否所有版本号都经过完整 Git 历史、版本标签、`package.json` 版本变更和更新日志交叉确认。
14. 是否所有版本号都只有三段，没有写入第四段修订号。
15. 是否在正文中重复描述了标准行已经写明的默认值。

## 输出结果

在项目 `docs/zh-cn/` 目录生成：

`FUNCTIONS.md`

文件开头使用：

```markdown
# 功能说明
```

然后按照 popup 面板结构继续编写。

不要在最终文档中写“根据代码分析”“根据文件”“实现逻辑位于”等开发者视角内容。

不要在最终文档中出现任务过程说明。

直接生成完整、干净、用户能看的功能说明文档。

## 标题 ID 要求

`FUNCTIONS.md` 中所有标题都必须添加稳定的 URL 定位 ID，方便在 GitHub 中通过链接直接跳转到对应位置。

这里的“所有标题”包括：

- `# 一级标题`
- `## 二级标题`
- `### 三级标题`
- `#### 四级标题`
- 以及文档中出现的其他所有 Markdown 标题

标题 ID 使用 GitHub 支持的自定义锚点格式，写在标题上一行：

```markdown
<a name="functions"></a>

# 功能说明

<a name="player"></a>

# 播放器

<a name="player-quality"></a>

## 锁定画质

<a name="player-quality-auto-switch"></a>

### 自动切换视频画质
```

ID 命名规则：

1. 只能使用小写英文、数字和短横线。
2. 不要使用中文。
3. 不要使用空格。
4. 不要使用下划线。
5. 不要使用特殊符号。
6. ID 必须尽量简短，但要能看出含义。
7. 同一个文档中不能出现重复 ID。
8. 如果标题属于某个分类，ID 应体现层级关系。

推荐命名方式：

```markdown
<a name="player"></a>

# 播放器

<a name="player-quality"></a>

## 锁定画质

<a name="player-quality-auto-switch"></a>

### 自动切换视频画质

<a name="player-speed-button"></a>

## 倍速按钮

<a name="player-speed-button-show"></a>

### 显示倍速按钮/倍速滑块
```

固定要求：

```markdown
<a name="functions"></a>

# 功能说明
```

文档开头的 `# 功能说明` 必须使用 `functions` 作为定位 ID。

如果存在同名标题，不能复用同一个 ID，必须根据所属分类生成不同 ID。

例如：

```markdown
<a name="player-layout-hide-button"></a>

### 隐藏按钮

<a name="shorts-layout-hide-button"></a>

### 隐藏按钮
```

最终文档中的每一个标题前面，都必须有一行对应的 `<a name="..."></a>`。

# 链接的补全

在进行完上述所有步骤后，需要对 `src\entrypoints\popup\pages` 进行 `<DocsHelpLink anchor="xxx" />` 补全

- 只对本次新增的特性进行补全，不要对已经存在、或旧 `FUNCTIONS.md` 已经存在的功能进行补全。
- 对应的 anchor 需要和 `FUNCTIONS.md` 中的标题 ID 一致。
- 不要对已经存在的 `<DocsHelpLink anchor="xxx" />` 进行修改、删除、更新。
